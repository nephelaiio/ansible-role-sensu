---
dependency:
  name: galaxy
lint:
  name: yamllint
platforms:
  - name: localhost
driver:
  name: delegated
  options:
    managed: False
    ansible_connection_options:
      ansible_connection: local
      connection: local
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: server
verifier:
  name: testinfra
  lint:
    name: flake8
